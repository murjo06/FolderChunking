#include <string>
class chunker {
    public:
        string from;
        string to;
    
        chunker(string from, string to) {
            from = from;
            to = to;
        }
    
        int StartChonk() {
            return 0;
        }
};