#include <array>
#include <string>
using namespace std;
class tree {
    public:
        //treeNode* nodes;
        int lenght;
        tree(string* values, string* parents, int len) {
            lenght = len;
            int i = 0;
            /*
            foreach(object value in values) {
                nodes[i] = new TreeNode(value, parents[i]);
                i++;
            }
            */
        }
};